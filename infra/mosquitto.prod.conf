# Prod MQTT-брокер: слушает ТОЛЬКО localhost — наружу 1883 не торчит.
# Приложение коннектится на 127.0.0.1:1883 (MQTT_URL). В firewall порт 1883 НЕ открывать.
listener 1883 127.0.0.1
allow_anonymous true
max_inflight_messages 100
